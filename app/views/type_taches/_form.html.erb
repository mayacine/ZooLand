<%= form_with(model: type_tach, local: true) do |form| %>
  <% if type_tach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_tach.errors.count, "error") %> prohibited this type_tach from being saved:</h2>

      <ul>
      <% type_tach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :libelle %>
    <%= form.text_field :libelle, id: :type_tach_libelle %>
  </div>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code, id: :type_tach_code %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
