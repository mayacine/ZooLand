<p id="notice"><%= notice %></p>

<h1>Type Taches</h1>

<table>
  <thead>
    <tr>
      <th>Libelle</th>
      <th>Code</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @type_taches.each do |type_tach| %>
      <tr>
        <td><%= type_tach.libelle %></td>
        <td><%= type_tach.code %></td>
        <td><%= link_to 'Show', type_tach %></td>
        <td><%= link_to 'Edit', edit_type_tach_path(type_tach) %></td>
        <td><%= link_to 'Destroy', type_tach, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Type Tach', new_type_tach_path %>
