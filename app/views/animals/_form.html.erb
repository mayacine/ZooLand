<%= form_with(model: animal, local: true) do |form| %>
  <% if animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
      <% animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom, id: :animal_nom %>
  </div>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code, id: :animal_code %>
  </div>

  <div class="field">
    <%= form.label :type_animal_id %>
    <%= form.text_field :type_animal_id, id: :animal_type_animal_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
